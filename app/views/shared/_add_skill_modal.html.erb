<% if user_signed_in? %>
  <div id="skillsModal" class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Te gustaria agregar un skill a tu perfil?</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <p>Los skills te van a permitir aplicar a proyectos y filtrar aquellos a los que puedes aplicar segun sus requerimientos</p>
        </div>
        <div class="modal-footer">
          <%= link_to "Agregar mi primer skill", new_user_skill_path(current_user), class:"btn btn-primary" %>
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Despues</button>
        </div>
      </div>
    </div>
  </div>
<% end %>

