<div>
  <h2 class="new-role-title">Roles de este proyecto</h2>
  <% @project_roles.each do |role| %>
    <div class="role-card">
      <%= role.name %>

    </div>
  <% end %>

</div>


<div class="new-role-container">

  <h4>Agregá un nuevo rol</h4>
  <%= simple_form_for([ @project, @role ], remote: true) do |f| %>
    <%= f.input :name %>
    <%= f.submit "Submit role", class: "btn btn-primary" %>
  <% end %>

</div>
